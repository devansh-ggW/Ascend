plugins {
    id 'com.android.application'
}

android {
    namespace 'com.ascend.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.ascend.app'
        minSdk 26
        targetSdk 35
        versionCode 4
        versionName '0.4.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
